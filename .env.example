# Postgres (Neon, Supabase, or local)
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Admin: if set, /admin requires this key (unlock form)
# ADMIN_SECRET=your-secret-here

# API key: if set, POST endpoints (create feed, subscription, poll) require
# Authorization: Bearer <WAKENET_API_KEY>. GET endpoints stay open.
# WAKENET_API_KEY=your-api-key-here

# Inngest (optional for local; use Inngest Dev Server)
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# App URL (for webhook signing verification docs)
NEXT_PUBLIC_APP_URL=http://localhost:3000
